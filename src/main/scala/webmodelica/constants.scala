package webmodelica

object constants {
  import java.nio.charset.StandardCharsets
  val encoding = StandardCharsets.UTF_8
  val confDefault = "webmodelica.conf"
  val projectCollection = "projects"
  val userCollection = "users"
  val authorizationHeader = "Authorization"
}
